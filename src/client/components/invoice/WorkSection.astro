---
const items = Array.from({ length: 16 }, (_, i) => ({
  img: `api/Images/Thumbnail${i + 1}.jpg`,
  title: `Thumbnail ${i + 1}`,
  price: "₹200",
}));
---

<section class="section work">
  <h2 class="section-title">Work Delivered</h2>

  <div class="work-grid">
    {items.map(item => (
      <div class="work-item">
        <img src={item.img} class="work-media" />
        <div class="work-content">
          <div class="work-category">Thumbnail Design</div>
          <div class="work-title">{item.title}</div>
          <div class="work-meta">
            <div class="work-date">October 2025</div>
            <div class="work-price">{item.price}</div>
          </div>
        </div>
      </div>
    ))}
  </div>
</section>
